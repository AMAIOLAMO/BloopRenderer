#include "Camera.h"

using namespace Bloop;

Camera::Camera(const Float3& position, const Float3& viewDirection) : position(position), viewDirection(viewDirection) {  }

/*Float3 Camera::RayFromUV(const Float2 &uv, const UInt2& resolution) const
{

}*/
